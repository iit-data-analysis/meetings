(function () {
    angular
            .module('meetings')
            .controller('meetingsController', meetingsController);

    function meetingsController() {
        activate();
        
        function activate() {
        }

    }
})();